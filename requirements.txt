# Python dependencies for generate_cv.py (see CLAUDE.md).
#
#   pip install -r requirements.txt
#
# Pinned to the 1.x line: generate_cv.py imports
# bibtexparser.customization.splitname, which the 2.x rewrite removed.
bibtexparser>=1.4.0,<2
